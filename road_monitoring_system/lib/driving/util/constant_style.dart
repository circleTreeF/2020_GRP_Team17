
import 'package:flutter/material.dart';

///The color of the data displayed.
const defaultDataColor = Color.fromRGBO(25, 26, 79, 1.0);
///The color of the title of the data displayed.
const defaultTitleColor = Color.fromRGBO(27, 42, 107, 1.0);
///The color of stop and start button.
const defaultButtonColor = Color.fromRGBO(25,26,79,1.0);
///The color of the border of the stop and start button
const defaultBorderColor = Color.fromRGBO(0,155,189,1.0);

///The text style of the data
const CardDataStyle = TextStyle(
    color: defaultDataColor,
    fontFamily: "Poppins-Bold",
    fontSize: 18,
    letterSpacing: 1.0
);

///The text style of the title
const CardTitleStyle = TextStyle(
      color: defaultTitleColor,
      fontFamily: "Poppins-Bold",
      fontSize: 17,
      letterSpacing: 1.0
);